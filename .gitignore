# cmake
build
# ASan 版构建目录 (2026-09: 查"对弈 x 后台训练"的堆损坏时建的, 见 issues_review C24)。
# 保留它可以让 repro_concurrency 随时复跑而不必重新编译 (~10 分钟), 但二进制不该进仓库。
build-asan
.qt
.vscode
.claude
*.user

# 训练权重 (运行期产物, 每个文件约 15 MB; GUI 与基准都从运行目录的 weights/ 读写)
# 验证用的权重可用 bench_ppo_mt --save=<prefix> 重新生成, 见 docs/training_optimization.md §7
weights/
